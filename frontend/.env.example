VITE_API_URL=http://localhost:8000/api/v1

# Stessa chiave pubblica generata nel backend con: php artisan webpush:vapid
# (backend/.env, VAPID_PUBLIC_KEY). E' una chiave pubblica: nessun problema
# a comparire nel bundle frontend.
VITE_VAPID_PUBLIC_KEY=
